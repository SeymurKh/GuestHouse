# Local development database
DATABASE_URL="file:./dev.db"

# Admin access credentials
ADMIN_PASSWORD=your-admin-password
ADMIN_TOKEN=your-admin-token   # Required for production. Use a long random token.

# Optional: if you deploy to a platform with a different database, override DATABASE_URL accordingly.
